[project]
name = "ml-workflow"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "black==25.9.0",
    "isort==5.12.0",
    "flake8==6.0.0",
    "pre-commit>=4.0.0",
]

[tool.black]
line-length = 120

[tool.isort]
profile = "black"
line_length = 120
